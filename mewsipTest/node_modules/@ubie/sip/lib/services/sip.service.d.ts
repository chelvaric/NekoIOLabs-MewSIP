import { EventEmitter } from '@angular/core';
import { SipConfig } from '../interfaces/sip-config';
export declare class SipService {
    OnRegistered: EventEmitter<boolean>;
    OnIncommingCall: EventEmitter<string>;
    OnMediaRecieved: EventEmitter<MediaStream>;
    private UserAgent;
    private IncommingCallSession;
    private InCall;
    constructor();
    acceptCall(): void;
    refuseCall(): void;
    setConfig(config: SipConfig): void;
    private registerAgentEvents;
    private registeredCallBack;
    private registerFailedCallBack;
    private inviteCallBack;
    private registerInviteEvents;
    private trackAddedCallback;
}
